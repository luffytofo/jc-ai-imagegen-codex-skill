interface:
  display_name: "JC AI Imagegen"
  short_description: "Generate or edit images through JC AI"
  default_prompt: "Use $jc-ai-imagegen to generate or edit an image using only this thread's visible prompt and attachments."

policy:
  allow_implicit_invocation: true
