interface:
  display_name: "JC AI Imagegen"
  short_description: "Generate or edit images through JC AI"
  default_prompt: "Use $jc-ai-imagegen to generate or edit an image from this request. Use external images only when I provide or authorize them."

policy:
  allow_implicit_invocation: true
